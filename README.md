# FN软仓客户端

> 飞牛OS第三方应用商店客户端

---

## 📖 应用简介

FN软仓是飞牛OS上的第三方应用商店客户端，支持**多软件源聚合**。用户可以自由添加自托管服务端地址，浏览、安装、更新飞牛官方应用中心没有的三方应用。

- **客户端项目地址**：[https://gitee.com/hhxs2025/fn-appstores](https://gitee.com/hhxs2025/fn-appstores)
- **服务端镜像**：`docker pull ccr.ccs.tencentyun.com/hhxs2025/fn-appstores-server:2.8.1`
- **平台**：飞牛OS
- **开源协议**：MIT

---

## ✨ 核心功能

### 应用管理

- **多源聚合**：从所有启用的软件源并发拉取应用列表，按 `id@platform` 自动去重，保留较新版本
- **树状聚合**：官方源可聚合多个三方源应用，用户无需自行添加即可发现更多应用
- **应用类型筛选**：按「全部 / Docker应用 / 原生应用」分类
- **按源名称筛选**：下拉框快速过滤特定来源的应用
- **分类标签**：影音、办公、下载、AI、设计、社交、网络、工具、生活、商务、教育、效率、开发、游戏
- **排序**：支持按名称、下载量、更新时间排序
- **搜索**：按应用名称、描述、作者关键词搜索，输入防抖 200ms
- **架构自动适配**：启动时检测本机架构，不兼容当前设备的应用自动过滤

### 安装与更新

- **一键安装**：支持安装向导、环境变量填写
- **实时进度**：WebSocket 推送下载进度、速度、已下载/总大小
- **独立进度弹窗**：大图标 + 大百分比数字 + 速度详情，成功自动关闭
- **一次性下载**：单次 GET 完整下载，不依赖 HEAD / Content-Length / Range，适配外链托管（Gitee 等）
- **格式校验与重试**：下载后校验 gzip 魔数，非有效 FPK 自动重试（最多 3 次），自动跨过 Gitee CDN 冷启动
- **进度自适应**：`Content-Length` 已知时显示百分比，未知时显示"已下载 X MB"
- **断线续传**：安装请求加入任务队列，WebSocket 断线重连后自动续传
- **超时复位**：安装 30 分钟未响应自动复位队列
- **安装即时刷新**：安装完成后自动更新卡片状态
- **客户端自更新**：设置页一键检测最新版本，下载 FPK 后引导手动安装

### 应用评分

- **5 星评分**：详情弹窗内点击星星打分
- **本地记录**：记录"我的评分"，再次打开显示已评星级
- **评分徽章**：卡片动作区显示平均分和评分人数
- **防刷机制**：基于客户端 ID 提交，转发到应用所属源服务端

### 软件源管理

- **添加/删除**：填写名称和地址，测试连通性后添加
- **启用/禁用**：三方源可随时启用或禁用
- **状态检测**：源列表显示在线/离线/已禁用，状态缓存 5 分钟
- **官方源保护**：官方源不可删除，地址隐藏显示

### 界面与体验

- **飞牛统一网关**：通过 `/app/fn-appstores-client` 路径访问，复用飞牛登录态，自动适配 HTTPS
- **HTTPS 图片代理**：HTTPS 页面下 HTTP 图片自动走同源代理，绕开浏览器混合内容拦截
- **开发者星球**：3D 球面可视化开发者，拖动旋转、滚轮缩放、悬停查看、点击进入详情
- **首页看板**：轮播图 + 公告记录 + 四列快捷卡片（已安装/可更新/热度榜/新应用）
- **轮播图点击放大**：点击轮播图查看大图，支持左右切换、ESC 关闭
- **骨架屏加载**：应用列表首次加载时显示灰色占位卡片，替代空白"加载中..."提示
- **卡片图标懒加载**：列表图片按需加载，首屏更快
- **深色/浅色主题**：一键切换，偏好保存至浏览器
- **数据持久化**：源配置存储在 `TRIM_PKGVAR` 目录，应用更新后配置不丢失

---

## 📜 版本变化

### v2.8.1
-设置页：新增建源指南
-开发者页：修复星球视图下搜索开发者功能无效的问题(搜索开发者时，被搜索对象白色光圈高亮，其余开发者星光淡化)

### v2.8.0

- 新增应用评分系统，支持 5 星评分
- 详情弹窗内新增评分区，可 hover 预览、点击提交
- 本地记录"我的评分"，再次打开显示已评星级
- 应用卡片动作区新增评分徽章，显示平均分和评分人数
- 新增客户端 ID 生成与持久化，用于评分防刷
- 新增架构自动检测，启动时判断本机是 x86 还是 ARM
- 应用列表按架构自动过滤，不兼容的应用不显示
- 应用字段 `arch` 统一改为 `platform`
- 后端新增 `rating.go`，评分请求转发到应用所属源服务端
- 应用列表加载时显示骨架屏占位卡片
- 搜索框加入 200ms 防抖，输入不再逐字触发全量重绘
- 应用卡片图标懒加载（`loading="lazy"`）
- **下载策略为一次性 GET**，适配 Gitee 外链托管场景
- **新增 gzip 魔数校验 + 自动重试（最多 3 次）**，跨过 Gitee CDN 冷启动
- **进度显示自适应**：`Content-Length` 未知时显示"已下载 X MB"
- 移除应用卡片上的平台徽章（详情弹窗保留）
- 修复首页快捷卡片图标在 HTTPS 下不显示的 bug

### v2.7.0

- 后端从 Python 全面迁移到 Go + Gin
- 编译为单二进制，无运行时依赖，启动更快、内存更低
- 支持 amd64 / arm64 交叉编译
- 模板引擎从 Jinja2 改为 Go html/template
- 新增开发者星球页面，3D 球面可视化开发者
- 开发者以星星形式分布在球面上，大小与应用数挂钩
- 三种配色区分官方/认证/个人开发者
- 支持拖动旋转、滚轮缩放、悬停查看、点击进入详情
- 窄屏（<900px）自动降级为卡片列表
- 新增独立安装进度弹窗，大图标 + 大百分比 + 速度详情
- 新增安装 30 分钟超时强制复位
- 新增 `SortOrderFlex` / `ScreenshotsFlex` 类型容错
- 网关模式改为同时注册"挂根"和"带前缀"两套路由
- 新增向导会话自动清理，每 10 分钟扫描超 30 分钟的会话
- 版本比较支持 `v` / `V` 前缀和多种分隔符

### v2.6.2

- 接入飞牛统一网关，应用改走 Unix Socket
- 通过 `/app/fn-appstores-client` 路径访问，不再监听 TCP 端口
- 复用飞牛登录态，自动注入用户身份 Header
- 自动适配 HTTPS，页面与 WebSocket 走 `https://` / `wss://`
- 桌面图标直达，点击即开，不用记端口
- 新增 HTTPS 图片代理，解决混合内容拦截
- 新增轮播图点击放大
- 应用卡片图标、详情截图、快捷卡片图标统一走图片代理
- 生命周期脚本适配网关，启动前清理残留 socket
- 启动后轮询等待 socket 生成（最多 10 秒）
- `status` 同时校验进程和 socket
- 保留端口模式，通过 `FN_SOFT_START_MODE=port` 降级

### v2.6.1

- WebSocket 统一管理，移除双定义冲突
- 动态协议适配 HTTPS，指数退避重连（最大 10 次）
- 安装任务队列，断线重连后请求自动续传
- 缓存读写统一入口，引入缓存版本号
- 安装/刷新成功后立即写入缓存
- 并发加载锁，防止重复请求
- 首页快捷卡片增加数据就绪检查
- DOM 空值保护全面加固，避免元素缺失导致脚本中断
- 错误降级：网络失败时自动使用本地缓存
- 源列表加载失败时保留上次成功列表
- 首页公告加载失败时使用过期缓存兜底

### v2.6.0

- 应用列表支持按名称、下载量、更新时间排序
- 安装进度显示实时百分比数字
- 安装完成后自动更新卡片状态，无需整页刷新
- 设置页新增清除本地缓存按钮
- 设置页新增聚合申请入口
- 设置页新增 QQ 群二维码
- 详情弹窗固定 4:3 比例

### v2.5.2

- 官方源地址隐藏，移除禁用按钮
- 三方源启用/禁用逻辑修正
- Docker/原生筛选修复
- 新增按源名称筛选下拉框
- 恢复缓存标签显示
- 新增「商务」「教育」分类标签
- 新应用卡片增至 10 个
- 版本号动态化

---

## 🧭 导航页介绍

左侧导航栏共 5 项，桌面端 140px 宽，窄屏（≤1100px）折叠为单字图标，移动端（≤768px）折叠为汉堡菜单。

### 🏠 首页

默认打开的页面，展示平台整体概况：

- **顶部轮播图**（占 70% 宽度）：平台宣传图，点击可查看大图
- **公告记录**（占 30% 宽度）：显示最近 5 条公告的日期和标题，点击查看详情，支持富文本
- **四列快捷卡片**：
  - **已安装**：列出所有已安装的应用
  - **可更新**：汇总所有有新版本的应用
  - **热度榜**：按下载量排序的 Top 10
  - **新应用**：按更新时间排序的最近 10 个应用

### 📦 应用

应用商店主页面：

- **工具栏**：状态指示 + 应用数量 + 排序下拉框 + 刷新按钮
- **搜索栏**：按名称、描述、作者搜索，输入防抖 200ms
- **分类标签**：15 个固定分类快速筛选
- **类型筛选**：全部 / Docker应用 / 原生应用
- **源筛选**：按来源过滤
- **应用网格**：卡片显示图标、名称、版本、作者、来源、下载量、评分徽章
- **骨架屏**：首次加载显示灰色占位卡片
- **分页**：每页 8 个应用

### ✨ 开发者

3D 球面可视化开发者（v2.7.0 新增）：

- 开发者以星星形式均匀分布在球面上
- 星星大小与应用数挂钩
- 三种配色：金色（官方）、蓝色（认证）、白色（个人）
- **拖动**旋转球体
- **滚轮**缩放
- **悬停**星星查看开发者信息
- **点击**星星打开开发者详情
- 详情显示头像、应用数、总下载、最近更新、应用列表
- 窄屏自动降级为卡片列表

### 🔗 源管理

软件源配置页面：

- **添加源**：填写名称和地址，可先测试连通性
- **源列表**：显示所有已配置的源及其状态
- **官方源**：地址隐藏显示为 `●●●●●●●`，不可删除
- **三方源**：可启用、禁用、删除

### ⚙️ 设置

客户端配置页面，四个标签页：

- **通用**：深色模式开关、导出日志、清除本地缓存
- **系统**：客户端版本、系统架构、检查更新
- **关于**：开发者、开源协议、源码仓库、交流群、应用上架、聚合申请、官方源查询、QQ 群二维码
- **统计**：总应用数、已安装、可更新、已配置源

---

## 📥 下载策略说明

### 当前策略：一次性 GET

FPK 文件下载采用**单次 GET 完整下载**，不发送 HEAD、不依赖 `Content-Length`、不使用 `Range`。

**为什么这么设计**：客户端通过服务端 302 跳转到 Gitee 等外链托管平台下载。Gitee 的 CDN（Varnish）在文件首次被访问时处于"回源"状态：

- HEAD 可能不返回 `Content-Length`
- GET 可能返回错误页而非完整文件（约几十字节）
- 缓存预热后（几十秒到几分钟）才恢复正常

一次性下载 + gzip 魔数校验 + 最多 3 次重试，能自动跨过这个冷启动窗口：

1. 首次 GET → 拿到错误页（非 gzip）
2. 校验失败 → 等 2 秒后重试
3. 第二次 GET → CDN 已预热 → 拿到完整 FPK → 校验通过 → 解压安装

### 进度显示自适应

- `Content-Length` 已知（CDN 已缓存）：`1.2 MB/s · 3.2 MB / 8.5 MB`，百分比正常走动
- `Content-Length` 未知（CDN 冷启动）：`1.2 MB/s · 已下载 3.2 MB`，百分比保持 0%

两种情况下用户都能看到下载在**推进**，不会卡死在 `-0.0 MB`。



## 👨‍💻 开发者信息

| 项目 | 信息 |
| :--- | :--- |
| **开发者** | 晦华先生 |
| **联系方式** | 2303537063@qq.com |
| **开源地址** | [https://gitee.com/hhxs2025/fn-appstores](https://gitee.com/hhxs2025/fn-appstores) |
| **交流群** | 2154077576 |
| **开源协议** | MIT |

欢迎开发者自托管服务端接入 FN软仓生态，或为现有应用提供更新维护。如有疑问请联系作者。

